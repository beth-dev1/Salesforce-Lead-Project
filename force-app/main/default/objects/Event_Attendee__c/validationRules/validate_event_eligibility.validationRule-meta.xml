<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>validate_event_eligibility</fullName>
    <active>true</active>
    <description>Ensures that an Attendee can only be associated with an Event that is active, accepting attendees, and has not ended.</description>
    <errorConditionFormula>OR(
    Event__r.End_Date__c &lt;=  NOW(),
  NOT(Event__r.Live__c),
 Event__r.Remaining_Seats__c  = 0
)</errorConditionFormula>
    <errorDisplayField>Event__c</errorDisplayField>
    <errorMessage>Attendee can only be added to an active event that has not ended and still has available seats.</errorMessage>
</ValidationRule>
