<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>validate_speaker_event_eligibility</fullName>
    <active>true</active>
    <description>Prevents associating a Speaker with an Event that is not live or has already ended.</description>
    <errorConditionFormula>AND(Event__r.End_Date__c &lt;=  NOW(),
 NOT( Event__r.Live__c )
)</errorConditionFormula>
    <errorDisplayField>Event__c</errorDisplayField>
    <errorMessage>Speaker can only be added to an active event that has not ended.</errorMessage>
</ValidationRule>
